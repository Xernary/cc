WS_1.03 - ZipZap - level 1
dal codice python si osserva che tutti i nomi dei zip creati nel server sono generati random e sanitizzati. L'unica eccezione sono i nomi dei file all'interno degli zip, ovvero il nome del file originale prima di essere zippato. quindi nominando il file "random_name & ping my_address" ad esempio possiamo eseguire il comando ping dal server, perchè esso esegue il comando zip + zip_path + my_file_name quindi tramite & possiamo eseguire il nostro comando in parallelo, in questo caso ping o qualsiasi altro comando
